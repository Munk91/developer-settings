# Atuin history DB / secrets
**/atuin/history.db
**/.local/share/atuin/**
*.key
*.session

# Git config (contains personal info)
dotfiles/git/.gitconfig

# Raycast exports are fine to commit, but ignore random dumps
*.rayconfig.backup

# macOS noise
.DS_Store

# Node / Python build caches if you ever add projects
node_modules/
__pycache__/

# Secrets (never commit)
.zsh_secrets
